apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: openlegal-server-config
    files:
      - server.toml
# Preserve the content hash across serving and independently applied admin Jobs.
