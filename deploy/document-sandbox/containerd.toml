# The selected K3s node already provides the `runc` CRI handler. No additional
# containerd handler is merged. Verify the generated config and RuntimeClass
# handler on the real node before applying the worker admission label.
