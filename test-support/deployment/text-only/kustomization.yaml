apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: openlegal-serving
resources:
  - namespace.yaml
  - deployment.yaml
configMapGenerator:
  - name: openlegal-server-config
    files:
      - server.toml
# Keep the content hash: config changes must replace the Pod through Recreate.
